def hash_tokens(tokens: list[int], window_size: int) -> str:
    """
    Hash tokens using rolling hash.

    Args:
        tokens: The list of tokens to hash.
        window_size: The size of the rolling window.

    Returns:
        A string representation of the hash.
    """
    ...
