def hamming_distance(a: str, b: str) -> float:
    """
    Compute hamming distance between two strings.

    Args:
        a: The first string.
        b: The second string.

    Returns:
        A float representing the hamming distance between the strings.
    """
    ...

def hamming_similarity(a: str, b: str) -> float:
    """
    Compute hamming similarity between two strings.

    Args:
        a: The first string.
        b: The second string.

    Returns:
        A float representing the hamming similarity between the strings.
    """
    ...

def levenshtein_distance(a: str, b: str) -> float:
    """
    Compute levenshtein distance between two strings.

    Args:
        a: The first string.
        b: The second string.

    Returns:
        A float representing the levenshtein distance between the strings.
    """
    ...

def normalized_levenshtein_distance(a: str, b: str) -> float:
    """
    Compute normalized levenshtein distance between two strings.

    Args:
        a: The first string.
        b: The second string.

    Returns:
        A float representing the normalized levenshtein distance between the strings.
    """
    ...

def levenshtein_similarity(a: str, b: str) -> float:
    """
    Compute levenshtein similarity between two strings.

    Args:
        a: The first string.
        b: The second string.

    Returns:
        A float representing the levenshtein similarity between the strings.
    """
    ...

def osa_distance(a: str, b: str) -> float:
    """
    Compute osa distance between two strings.

    Args:
        a: The first string.
        b: The second string.

    Returns:
        A float representing the osa distance between the strings.
    """
    ...

def osa_similarity(a: str, b: str) -> float:
    """
    Compute osa similarity between two strings.

    Args:
        a: The first string.
        b: The second string.

    Returns:
        A float representing the osa similarity between the strings.
    """
    ...

def damerau_levenshtein_distance(a: str, b: str) -> float:
    """
    Compute damerau levenshtein distance between two strings.

    Args:
        a: The first string.
        b: The second string.

    Returns:
        A float representing the damerau levenshtein distance between the strings.
    """
    ...

def damerau_levenshtein_similarity(a: str, b: str) -> float:
    """
    Compute damerau levenshtein similarity between two strings.

    Args:
        a: The first string.
        b: The second string.

    Returns:
        A float representing the damerau levenshtein similarity between the strings.
    """
    ...
